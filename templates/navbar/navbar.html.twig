<nav id="navbar" class="bg-blue navbar m-0">


    <h1>
        <a class="" href="{{ path('home') }}">
            <img class="navbar-brand nav-link m-0" src="/docs/4.3/assets/brand/bootstrap-solid.svg" class="d-inline-block align-top" alt="">
            <span class="text-white">pixel</span>
            <span class="text-green">market</span>
        </a>
    </h1>

    <form class="search-bar">
        <input
        class="search form-control mr-sm-2 w-100 rounded-pill border-light bg-secondary text-white" type="search" placeholder="Rechercher" aria-label="Search">
    <!-- <button class="btn btn-outline-success my-2 my-sm-0" type="submit">GO</button> -->
    </form>

    <ul id="toogle-sign">
        <li>
            <a class="nav-link border-0 rounded-pill bg-green text-blue p-1 px-4" href="#">Créer une annonce</a>
        </li>
        {% if app.user is not empty %}
            <li class="nav-item dropdown" id="profileNavbar">
                <a class="nav-link dropdown-toggle text-white" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    <span class="h6 text-white ">
                        Bienvenue
                        {{ app.user.username }}
                    </span>
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <a class="dropdown-item" href="{{ path('user_profile', { 'id': app.user.id }) }}">Profile</a>
                    <a class="dropdown-item disabled" href="#">Settings</a>
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href="{{path("app_logout")}}">Log out</a>
                </div>
            </li>
        {% else %}
            <li>
                <a id="signInUp" class="nav-link border-0 rounded-pill bg-white text-blue p-1 px-4" href="{{path("app_login")}}">S'inscrire/Se connecter</a>
            </li>
        {% endif %}
    </ul>

    <div class="bg-blue" id="hamburger">
        {% include 'navbar/_navbar-burger.html.twig'%}
    </div>

</nav>
